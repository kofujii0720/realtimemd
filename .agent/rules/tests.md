---
paths:
  - "tests/**/*.ts"
  - "tests/**/*.py"
---

テストコードを作成する際は以下を厳守すること。
- テスト名の先頭に必ず対応観点IDを入れる（例: `it('[VP-001] ...')`, `def test_[VP-001]...`）。
- 単体テストでは観点消化計画表をコード作成より先に出力すること。
- モックは外部I/O（DB/HTTP/時刻）に限定し、ロジックは実体で検証すること（デトロイト派）。
